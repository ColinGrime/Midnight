group 'me.colingrimes.midnight-particles'

dependencies {
    compileOnly project(':midnight')
    testImplementation project(':midnight')
}

jar {
    archiveFileName = "MidnightParticles-${project.version}.jar"
}
