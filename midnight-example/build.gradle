import org.apache.tools.ant.filters.ReplaceTokens

dependencies {
    implementation project(':midnight')
}

// Allows ${version} placeholders.
processResources {
    filter ReplaceTokens, beginToken: '${', endToken: '}', tokens: ['version': version]
}
